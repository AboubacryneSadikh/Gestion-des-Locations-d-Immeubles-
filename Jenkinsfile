pipeline {
  
    agent any


    stages {

      stage('build') {
        steps {
          echo 'execution du code '
        }
      }

      stage('test') {
        steps {
          echo 'teste du code '
        }
      }

       stage('deployment') {
        steps {
          echo 'deploiement du code '
        }
      }
    }
}

